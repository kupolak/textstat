source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in textstat.gemspec
gemspec

# Local development gems that aren't needed for gem users
# Add any local-only development tools here if needed
